//Can use import instead of require like bellow
import React from "react";
import ReactDOM from "react-dom";

//How to create and display an HTML element
ReactDOM.render(
  <div>
    <h1>Hello World!</h1>
    <p>This is a paragraph</p>
  </div>,
  document.getElementById("root")
);
